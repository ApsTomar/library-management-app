export class Book {
    name: string
    subject: string
    authorID: string
    available: boolean
    availableDate: Date
}

export class User {
    id: string
    name: string
    email: string
    accountRole: string
    password: string
    token: string
}